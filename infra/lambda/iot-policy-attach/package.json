{
  "name": "joppilot-iot-policy-attach",
  "private": true,
  "version": "0.1.0",
  "description": "C3 (closes DEV-23): attaches the console-viewer IoT policy to the caller's Cognito identity on first operator sign-in. Deps installed by the terraform workflow (npm ci) before archive_file zips this directory. NOT a pnpm workspace member on purpose - the Lambda zip needs a plain node_modules layout.",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-cognito-identity": "3.1085.0",
    "@aws-sdk/client-iot": "3.1085.0"
  }
}
