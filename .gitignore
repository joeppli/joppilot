# ──────────────────────────────────────────────────────────────
# Joppilot monorepo .gitignore
# Goal: a fresh `git clone` + `pnpm install` + `docker compose up`
# must be enough to build & run every workspace on another machine.
# ──────────────────────────────────────────────────────────────

# ── Docs ──────────────────────────────────────────────────────
# .claude/  -> authoritative docs (ICD, architecture, constraints): TRACKED on purpose.
# .claude/settings.json -> EXCEPTION: per-machine Claude Code permissions
#   (session scratchpad paths, versioned skill dirs). Personal settings
#   belong in .claude/settings.local.json.
.claude/settings.json
.claude/settings.local.json
# .supp_docs/ -> supplementary material (translations, pdfs, decks): kept out of VCS.
.supp_docs/

# ── Dependencies ──────────────────────────────────────────────
node_modules/
.pnpm-store/

# ── Logs ──────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Prisma generated client ──────────────────────────────────
# Each service generates its own Prisma client into generated/prisma/.
# Recreated by `npx prisma generate` — never committed.
**/generated/prisma/

# ── npm lockfile (we use pnpm) ───────────────────────────────
package-lock.json
# EXCEPTION (M3-4a): the Lambda dirs are deliberately OUTSIDE the pnpm
# workspace — their zips need a plain node_modules layout, so the terraform
# workflow runs `npm ci` there, which REQUIRES a committed lockfile.
!infra/lambda/*/package-lock.json

# ── Build / cache output ─────────────────────────────────────
# NOTE: packages/contract/schemas/*.json are generated from Zod but
# intentionally committed — the Rust edge reads them at runtime and
# does not run the TS build. Do NOT ignore them here.
dist/
build/
out/
coverage/
.turbo/
.next/
.vite/
*.tsbuildinfo
.eslintcache

# ── Rust edge (Cargo) ────────────────────────────────────────
# Cargo.lock IS committed (binary crate) — only build output is ignored.
target/
**/*.rs.bk

# ── Environment files ────────────────────────────────────────
# Real env files stay local; *.example templates are committed so a
# clone can `cp services/<svc>/.env.example services/<svc>/.env`.
.env
.env.local
.env.*.local
!.env.example
!**/.env.example
# PC-A vehicle launcher config (filled per machine; vehicle.env.example is committed)
edge/vehicle.env

# ── Secrets / certificates (e.g. AWS IoT mTLS for the edge) ──
certs/
*.pem
*.key
*.crt
*.p12
*.pfx
# Greengrass core identity + filled installer config (machine-local)
edge/greengrass/gg-certs/
edge/greengrass/gg-config/
# Vehicle-thing cert bundle mounted into the core for the VEA component
edge/greengrass/veh-certs/

# ── Databases ─────────────────────────────────────────────────
*.sqlite
*.db

# ── IDE / OS ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Terraform (infra/terraform) ───────────────────────────────
# Local provider plugins, state caches, plan outputs, and any *.auto.tfvars with
# account-specific or secret values. Committed: *.tf, terraform.tfvars, .terraform.lock.hcl.
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.auto.tfvars
.terraformrc
terraform.rc

# M3-4a: archive_file zip output for the telemetry-ingest Lambda
infra/terraform/modules/*/.build/
