{
  "name": "@joppilot/contract",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run build:schemas",
    "build:schemas": "ts-node --transpileOnly scripts/build-schemas.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "zod-to-json-schema": "^3.22.4",
    "vitest": "^1.3.1"
  }
}
